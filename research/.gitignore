# Python env / caches
.venv*/
__pycache__/
*.pyc

# Datasets — NEVER pushed. Huge, and not ours to redistribute (PodcastFillers,
# Apple SEP-28k, your own recordings all live under data/).
data/
data-set/

# Trained weights + Core ML exports — regenerated by training, not versioned.
checkpoints/
*.pt
*.mlpackage

# Stray large media that might land outside data/ by accident.
*.wav
*.zip

# validate-tool scratch (your hand labels for a window)
labels.json
